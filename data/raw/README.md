# Raw data

Input data for training. Paths under `data/raw/` are the single source of truth for "what data this run used."

- `sample.csv` – minimal synthetic tabular data (two features, one target) for running the training script.
